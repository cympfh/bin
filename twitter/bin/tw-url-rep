#!/bin/bash

if [ $# -lt 3 ]; then
  echo "[usage] tw-rep-url screen_name status_id image_url"
  exit 0
fi

URL=$3
LOC="/tmp/${URL##*/}"
wget --quiet -O $LOC  $URL
twurl -X POST "/1.1/statuses/update_with_media.json" --file $LOC --file-field "media[]" -d "in_reply_to_status_id=$2" -d "status=@$1"
