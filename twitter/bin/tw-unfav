#!/bin/bash

usage() {
  cat << HELP
Usage: tw-unfav <id>

destroy favoriate for id
HELP
}

if [ $# -lt 1 -o "_$1" == "_-h" -o "_$1" == "_-?" ]; then
  usage
  exit 0
fi

twurl -X POST /1.1/favorites/destroy.json -d id=$1
