#!/bin/sh
# chat in bag-of-words
# Requirement: tw (twurl), mecab

if [ -n "$1" ]; then
  msg=$1
else
  read msg
  while read line; do
    msg="$msg\n$line"
  done
fi

echo $msg | mecab -Owakati | sed 's/ /\n/g' | sort | tr '\n' ' ' | tw

